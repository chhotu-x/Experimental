<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="<%= (meta && meta.description) || '' %>">
    <link rel="canonical" href="<%= (meta && meta.canonical) || '' %>">
    <!-- Open Graph / Twitter -->
    <meta property="og:title" content="<%= (meta && (meta.ogTitle || title)) || '' %>">
    <meta property="og:description" content="<%= (meta && meta.ogDescription) || (meta && meta.description) || '' %>">
    <meta property="og:type" content="<%= (meta && meta.type) || 'website' %>">
    <meta property="og:image" content="<%= (meta && meta.ogImage) || '' %>">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= (meta && (meta.ogTitle || title)) || '' %>">
    <meta name="twitter:description" content="<%= (meta && meta.ogDescription) || (meta && meta.description) || '' %>">
    <meta name="twitter:image" content="<%= (meta && meta.ogImage) || '' %>">
    <!-- Bootstrap CSS -->
    <link href="https://unpkg.com/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" onerror="this.href='/css/bootstrap-fallback.css'">
    <!-- Font Awesome CSS -->
    <link href="https://use.fontawesome.com/releases/v6.0.0/css/all.css" rel="stylesheet" onerror="this.href='/css/fontawesome-fallback.css'">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="fas fa-code text-primary"></i> 42Web.io
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'home' ? 'active' : '' %>" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'about' ? 'active' : '' %>" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'services' ? 'active' : '' %>" href="/services">Services</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'blog' ? 'active' : '' %>" href="/blog">Blog</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'demo' ? 'active' : '' %>" href="/demo">Demo</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'contact' ? 'active' : '' %>" href="/contact">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <%- body %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5><i class="fas fa-code text-primary"></i> 42Web.io</h5>
                    <p class="text-muted">Innovative tech solutions for modern businesses.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <h6>Follow Us</h6>
                    <a href="#" class="text-light me-3"><i class="fab fa-twitter"></i></a>
                    <a href="#" class="text-light me-3"><i class="fab fa-linkedin"></i></a>
                    <a href="#" class="text-light me-3"><i class="fab fa-github"></i></a>
                </div>
            </div>
            <hr class="border-secondary">
            <div class="text-center text-muted">
                <p>&copy; 2025 42Web.io. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script>
        function loadScript(src) {
            var script = document.createElement('script');
            script.src = src;
            document.head.appendChild(script);
        }
    </script>
    <script src="https://unpkg.com/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" onerror="loadScript('/js/bootstrap-fallback.js')"></script>
    <script src="/js/main.js"></script>
</body>
</html>
